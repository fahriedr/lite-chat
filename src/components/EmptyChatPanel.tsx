import Cookies from "js-cookie";
import React from "react";

const EmptyChatPanel = (data: any) => {

  return (
    <div className="flex justify-center place-items-center bg-[#202C33] w-full">
      <span>Start Conversation Now</span>
    </div>
  );
};

export default EmptyChatPanel;

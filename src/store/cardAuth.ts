// import { create } from 'zustand'

// type Store = {
//     card: 'login' | 'register'
//     action: (props: string) => void
// }

// const useStore = create<Store>()((set) => ({
//     card: 'login',
//     action: (props) => set((state) => ({ card: state. })),
//   }))